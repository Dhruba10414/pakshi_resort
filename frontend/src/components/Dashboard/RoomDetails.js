import React from 'react'

function RoomDetails({id, name, room_no}) {
    return (
        <div>
            {id}
            {name}
            {room_no}
        </div>
    )
}

export default RoomDetails
